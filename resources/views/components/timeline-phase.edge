{{--  Usage:
  @component('components/timeline-phase', {start: '2014', end: '2016'})
    <h3 class="m-0">
      Hi hi this appears as the main section
    </h3>
  @endcomponent
    --}}

<div class="py-8 px-2 md:px-6 print:px-8 print:py-8 w-full space-y-6 print:space-y-3 border-primary-400 border rounded-xl">
  <div>
    <span class="text-xl text-center font-medium">{{start}} ➡ {{end}}</span>
  </div>
  <div class="flex items-center">
    {{{ $slots.main ? await $slots.main() : '' }}}
  </div>
</div>
